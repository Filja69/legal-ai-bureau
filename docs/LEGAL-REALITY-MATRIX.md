# LEGAL AI BUREAU — Reality Matrix

Companion to `docs/PHASE-9-AUDIT.md`; updated 2026-08-14 after Phase 9.3 (Litigation & Case Intelligence — see `docs/PHASE-9-3-LITIGATION-RESULT.md`). Legend: **REAL** = working code against real logic/data path (may still use a mock data source underneath — see the Data column); **REAL / LIVE VERIFIED** = exercised this session against a real running Postgres and/or a real browser session, not just unit-tested; **MOCK** = the entire capability is synthetic/demo; **ADAPTER_ONLY** = a real HTTP client class exists but has never received a real response from the live source; **BLOCKED** = real code exists but cannot be exercised this session for an infrastructure reason (credentials/network/Docker); **NOT_IMPLEMENTED** = no working code, honest 501 or absent.

| Capability | Reasoning logic | Data source | Status | Notes |
|---|---|---|---|---|
| Legal Research (fact extraction → IRAC → confidence) | REAL | Mock legal dataset | REAL (mock data) | Full pipeline exercised by 30+ integration tests including adversarial hallucination cases |
| Contract Intelligence (clause/risk/redline) | REAL | Mock contract dataset (5 types) | REAL (mock data) | Two-lawyer review, word-level diff, explicit accept/reject |
| Citation validation | REAL | `LawVersion`/`LegalSource` tables (mock-populated) | REAL (mock data) | Temporal + existence checks, `TEMPORALLY_INVALID`/`MOCK`/`VERIFIED` outcomes |
| Hybrid retrieval (keyword+vector+RRF) | REAL | pgvector + Postgres FTS | REAL | Never benchmarked against real embeddings (see below) |
| Anthropic structured generation | REAL | Anthropic API | UNVERIFIED LIVE | Never exercised with a real API key in any session |
| OpenAI structured generation | REAL | OpenAI API | UNVERIFIED LIVE | Same |
| OpenAI embeddings (`text-embedding-3-small`) | REAL | OpenAI API | UNVERIFIED LIVE | Same; mock-embedding baseline captured, no real-vs-mock benchmark comparison exists |
| Official law source (`pravo.gov.ru`) | REAL client | Live government site | ADAPTER_ONLY | Unreachable from every sandboxed session to date, including this one (not re-attempted this session — see audit §13) |
| Court practice source (`kad.arbitr.ru`) | Stub client (25 lines) | — | ADAPTER_ONLY | No real HTTP integration written yet |
| Tax source (ФНС) | Stub client (26 lines) | — | ADAPTER_ONLY | Same |
| Commercial legal DB source | Stub client (39 lines) | — | ADAPTER_ONLY | Same |
| JWT authentication | REAL | — | REAL / LIVE VERIFIED | Fixed Phase 9: insecure default secret now fails startup in production; live-verified this session via real browser login → JWT → workspace-scoped dashboard |
| Workspace/tenant isolation | REAL | — | REAL / LIVE VERIFIED | 12 dedicated tests (7 pre-existing + 5 Phase 9.2 document-tenant-isolation) all passing against real Postgres this session, plus the structural "public KB has no workspace_id column" check and a direct SQL query confirming `embedding_chunks` has no `workspace_id` column at all |
| CORS policy | REAL | — | REAL | Fixed Phase 9: `CORSMiddleware` wired to `CORS_ALLOWED_ORIGINS`; not separately re-verified live this session (no cross-origin browser request was exercised) |
| Rate limiting | REAL | — | REAL | Fixed Phase 9: in-process limiter on `/auth/token` (per-IP) and LLM-cost endpoints (per-workspace); single-instance only, documented limitation; not re-exercised live this session |
| Document upload/storage | REAL | Local filesystem, tenant/document-id-scoped paths | REAL / LIVE VERIFIED | 25 MB streaming limit, magic-byte + extension/MIME-mismatch validation, ZIP-bomb defense; live-uploaded via real browser: TXT/DOCX/PDF/XLSX all `ready`, scanned PDF honestly `ocr_required`, a path-traversal-attempting filename (`../../../../etc/passwd.txt`) stored and displayed safely as inert text, never interpreted as a path |
| Document text extraction (PDF/DOCX/TXT/XLSX) | REAL | `pypdf`/`python-docx`/`openpyxl` | REAL / LIVE VERIFIED | Live-verified in-browser: TXT and DOCX Content tabs show real extracted text; PDF Overview shows real `page_count: 2`; a real bug was found and fixed this session (see result doc) where PDF's per-page provenance was silently lost under fallback chunking — now fixed and confirmed live (PDF shows 2 pages → 2 chunks, not 1) |
| OCR (scanned PDF) | — | — | NOT_IMPLEMENTED (explicitly out of scope) | Honestly reports `ocr_required` — live-verified: a synthetic no-text-layer PDF uploaded in-browser correctly shows the `OCR REQUIRED` badge, never a fake extraction |
| Deterministic clause/section structure detection | REAL | Regex over extracted text | REAL / LIVE VERIFIED | Numbered clauses ("4.1"), "Статья N", "§ N", "п. N"; live-verified: the TXT document (has numbered clauses) shows "Structure detected: Yes", the PDF (plain sentences) honestly shows "Structure detected: No (plain-text fallback)" with the warning surfaced in the UI |
| Tenant document chunking + indexing | REAL | `DocumentChunk` table (structurally separate from public `EmbeddingChunk`, always `workspace_id`-scoped) | REAL / LIVE VERIFIED | Direct SQL against the live DB confirms `document_chunks` has a `workspace_id` column and `embedding_chunks` does not, and zero rows in `embedding_chunks` share text with any `document_chunks` row |
| Document Q&A (`POST /documents/{id}/ask`) | REAL | `TenantDocumentRetriever` + `LLMGateway` | REAL / LIVE VERIFIED (mock LLM) | Live-verified in-browser: asking a real question against the real TXT document correctly returns "Insufficient document evidence… nothing was fabricated" — the two-gate evidence logic is real and live-exercised; the LLM-dependent "answers with citations" path is unit-tested only (fake gateway), since `LLM_PROVIDER=mock` structurally cannot produce `sufficient_evidence: true` |
| Document analysis (`POST /documents/{id}/analyze`) | REAL (dates/amounts/parties) + REAL (mock LLM for obligations/risks) | Regex + `LLMGateway` | REAL / LIVE VERIFIED (mock LLM) | Live-verified in-browser: running Analysis on the real TXT document correctly extracted `31.12.2026` tagged `EXTRACTED` with provenance `п.2.2`. Also surfaced a real, previously-undocumented limitation live: the deterministic amount/party regexes are Russian-pattern-only (`руб`, `ООО`, `Заказчик`, etc.) and did not match the English-language smoke-test document's "150000 RUB"/"Contractor:"/"Client:" — by design, not a bug, but worth flagging for any English-language contract usage |
| Contract-from-Document integration | REAL | `Document.extracted_text` → `POST /contracts` | REAL / LIVE VERIFIED | Covered by the live 512-test Postgres run (`test_create_contract_from_processed_document`, `test_create_contract_from_not_ready_document_returns_409`); not separately re-clicked through the browser this session |
| Research-with-documents integration | REAL | `TenantDocumentRetriever` → `case_context` | REAL / LIVE VERIFIED | Covered by the live 512-test Postgres run (`test_research_with_document_ids_includes_document_evidence_and_verifies_ownership`, incl. the cross-tenant 404 case) |
| Company profiles / registry lookup | — | — | NOT_IMPLEMENTED | No domain logic, no real connector; UI honestly shows "unavailable" |
| Due diligence reports | — | — | NOT_IMPLEMENTED | Same |
| Case parties / case-document linking | REAL | `CaseParty`/`CaseDocument` tables, reuses Phase 9.2 `Document` (never duplicated) | REAL / LIVE VERIFIED | Live-verified in-browser: three real documents (invoice/act/correspondence) attached to a real case with distinct `CaseDocumentRole` values |
| Case fact extraction (dates/amounts/parties) | REAL — 100% deterministic regex, zero LLM calls | `app/domains/litigation/fact_extractor.py` + `legal_patterns.py` (shared with Contract Intelligence) | REAL / LIVE VERIFIED | Every fact carries real provenance: `(document_id, chunk_id, page_number, excerpt)` from a literal regex match against literal chunk text. Live-verified: extracting facts from the adversarial fixture produced exactly 4 facts, each showing its real source excerpt in the UI |
| Case fact deduplication / corroboration | REAL — deterministic `(fact_type, normalized_value)` grouping, no semantic/embedding dedup | — | REAL / LIVE VERIFIED | Live-verified: the `10.03.2026` date fact shows `corroboration_count = 2` because it is independently asserted in both `invoice.txt` and `email.txt` |
| Case timeline construction | REAL — deterministic chronological sort, `infer_event_type()` keyword classifier | `CaseEvent` table | REAL / LIVE VERIFIED | Live-verified: two events (10 and 12 March 2026) rendered in chronological order, both tagged `EXACT` date type and `[delivery]` event type from the fixture text |
| Contradiction detection (date/amount) | REAL — bounded deterministic heuristics (event-type-grouped date mismatch; ≤5x-ratio amount mismatch), explicitly NOT party/document-number/performance-status | `app/domains/litigation/contradiction_detector.py` | REAL / LIVE VERIFIED | Live-verified against the brief's own worked adversarial example (§55): invoice 500,000 RUB/10 March vs. act 450,000 RUB/12 March correctly surfaced as one `date_mismatch` and one `amount_mismatch`, matching the proven integration test exactly |
| Evidence matrix (STRONG/MODERATE/CONFLICTED) | REAL — computed at read time, not persisted | — | REAL / LIVE VERIFIED | Live-verified: all 4 facts in the adversarial case correctly show `CONFLICTED` with the reason "Contradicted by another document in this case" |
| Claims/defenses, legal issue tree | — | — | NOT_IMPLEMENTED | Explicitly out of scope this phase — no persistence, no endpoint |
| Opponent modeling / counterargument engine | — | — | NOT_IMPLEMENTED | Explicitly out of scope this phase; frontend Strategy tab honestly states this rather than fabricating content |
| Procedural/limitation analysis | — | — | NOT_IMPLEMENTED | Explicitly out of scope this phase |
| Litigation strategy generation | — | — | NOT_IMPLEMENTED | `agents/litigation/agent.py` is still a Protocol stub; frontend Strategy tab shows an honest out-of-scope message, never fabricated |
| Case memo / draft procedural document generation | — | — | NOT_IMPLEMENTED | Explicitly out of scope this phase; frontend Drafts tab shows an honest out-of-scope message |
| Litigation eval suite (50-case) | — | — | NOT_IMPLEMENTED | Not built this phase; the one worked adversarial example from the brief is covered by a live-verified integration test instead |
| Corporate legal workflows | — | — | NOT_IMPLEMENTED | `agents/corporate/agent.py` is a 25-line Protocol stub only |
| Standalone legal document generation | — | — | NOT_IMPLEMENTED | No endpoint, no domain logic |
| Audit trail (queryable) | — | — | NOT_IMPLEMENTED | Events are logged via `structlog` only; no persisted, queryable `AuditLog` writer wired in despite the model existing |
| Celery/Redis task queue | — | — | NOT_IMPLEMENTED (dead dependency) | Declared in `pyproject.toml`, zero usage in `app/`; document processing is real synchronous in-request work (Phase 9.2 brief §24), not a queue |
| `alembic upgrade head` against live DB | — | — | REAL / LIVE VERIFIED | `alembic current` confirms `0010_litigation_intelligence (head)` applied on the dev DB this session; the migration's own `upgrade→downgrade→upgrade` cycle was verified on a throwaway scratch DB before applying to dev |
| DB-dependent tests (full suite) | — | — | REAL / LIVE VERIFIED | **548 passed, 0 failed** — full `pytest` run against real Postgres/pgvector this session (was 512 at the end of Phase 9.2.1; +36 from Phase 9.3) |
| Live end-to-end smoke test (Document Intelligence) | — | — | REAL / LIVE VERIFIED | Real browser session from Phase 9.2.1: login → JWT → dashboard → Documents list → TXT/DOCX/PDF detail tabs. See `docs/PHASE-9-2-INTEGRATION-VERIFICATION.md` |
| Live end-to-end smoke test (Litigation) | — | — | REAL / LIVE VERIFIED | Real browser session this session: login → create case → upload 3 synthetic adversarial documents (invoice/act/correspondence, mirroring the brief's §55 worked example) → attach with roles → Run Full Analysis → Facts tab shows 4 facts each with real source excerpts → Timeline tab shows 2 chronologically-sorted events → Evidence tab shows all 4 facts `CONFLICTED` and both the `date_mismatch` and `amount_mismatch` contradictions with both conflicting fact statements displayed → Strategy tab honestly states scope limits instead of fabricating. See `docs/PHASE-9-3-LITIGATION-RESULT.md` |
